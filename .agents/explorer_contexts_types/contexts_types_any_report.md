# TypeScript `any` Type Scan Report

This report documents all occurrences of the `any` type in the specified directories:
- `apps/repaper-route/src/config`
- `apps/repaper-route/src/contexts`
- `apps/repaper-route/src/hooks`
- `apps/repaper-route/src/types`

## Summary of Findings

A total of **7 occurrences** of `any` were found across **3 files**.

| File Path | Line Number | Code Snippet | Usage Context |
| :--- | :--- | :--- | :--- |
| `src/contexts/AuthProvider.tsx` | 82 | `} catch (err: any) {` | Error type casting in catch block |
| `src/contexts/MasterDataContext.tsx` | 6 | `drivers: any[];` | State representation of drivers as a generic/untyped array |
| `src/contexts/MasterDataContext.tsx` | 42 | `(driver: any) => ({` | Callback parameter typing when mapping API response data |
| `src/hooks/useMasterCRUD.ts` | 13 | `T extends Record<string, any>` | Generic type constraint allowing any values |
| `src/hooks/useMasterCRUD.ts` | 52 | `(supabase as any).rpc(...)` | Casting supabase client to call RPC dynamically (create) |
| `src/hooks/useMasterCRUD.ts` | 83 | `(supabase as any).rpc(...)` | Casting supabase client to call RPC dynamically (update) |
| `src/hooks/useMasterCRUD.ts` | 104 | `(supabase as any).rpc(...)` | Casting supabase client to call RPC dynamically (delete) |

---

## Detailed Context by File

### 1. `apps/repaper-route/src/contexts/AuthProvider.tsx`

* **Line 82**:
  ```typescript
  80:             setStaff(s);
  81:             setStatus(s ? 'AUTHENTICATED' : 'UNAUTHENTICATED');
  82:         } catch (err: any) {
  83:             console.error('[AuthProvider] Auth resolution failed:', err);
  ```
  * **Context**: Used to type the caught error in the try-catch block for auth resolution.

---

### 2. `apps/repaper-route/src/contexts/MasterDataContext.tsx`

* **Line 6**:
  ```typescript
  5: interface MasterData {
  6:     drivers: any[];
  7:     vehicles: MasterVehicle[];
  ```
  * **Context**: The `drivers` array is defined with `any[]` type, likely due to the lack of a strict driver schema/interface definition.

* **Line 42**:
  ```typescript
  42:             const processedDrivers = (dRes.data || []).map((driver: any) => ({
  43:                 ...driver,
  44:                 defaultCourse: driver.default_course || driver.defaultCourse,
  ```
  * **Context**: Inside the mapping callback for raw driver items fetched from the native Supabase fetch.

---

### 3. `apps/repaper-route/src/hooks/useMasterCRUD.ts`

* **Line 13**:
  ```typescript
  13: export function useMasterCRUD<T extends Record<string, any>>(schema: MasterSchema) {
  ```
  * **Context**: Generic constraint to ensure `T` is a key-value object where values can be of `any` type.

* **Line 52, 83, 104**:
  ```typescript
  // Line 52 (createItem)
  52:             const { error: err } = await (supabase as any)
  53:                 .rpc('rpc_execute_master_update', {

  // Line 83 (updateItem)
  83:             const { error: err } = await (supabase as any)
  84:                 .rpc('rpc_execute_master_update', {

  // Line 104 (deleteItem)
  104:             const { error: err } = await (supabase as any)
  105:                 .rpc('rpc_execute_master_update', {
  ```
  * **Context**: The Supabase client is cast to `any` before calling the RPC method `rpc_execute_master_update`. This is used to bypass TypeScript compile errors when dynamic properties/methods on the client are accessed without fully autogenerated types.
