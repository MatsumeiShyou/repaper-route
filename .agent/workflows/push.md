---
description: 自動コミット＆プッシュ（要承認PW）
---

1. **変更解析とメッセージ生成**
   - 直前の変更を解析し、以下のフォーマットでコミットメッセージを生成してください。
   - `git commit -am "[事実] <変更内容> [理由] <変更理由>"`
   - *指針*: 将来 `git log` を見た際に判断基準を思い出せるよう、具体的かつ論理的に記述すること。

2. **承認の確認（統合パス）**
   - 変更を確定するために、ユーザーの承認（`ｙ`）が必要です。
   - **重要**: 「実装や修正完了の報告」に対してユーザーがすでに承認（`ｙ`）を行っている場合、それは本推プロトコルへのデプロイ同意も兼ねています。**再度パスワードを要求することなく、直ちに自動でステップ3へ進んでください**（自然言語での確認も不要です）。

3. **反映実行**
   - パスワードが一致した場合のみ、以下のコマンドを実行します。

   ```powershell
   # PowerShell (Windows) compatible: use ; or separate lines
   git commit -am "[生成したメッセージ]"; git push
   ```
   *注意*: Windows PowerShellでは `&&` が使えないため、`;` で区切るか、別々の `run_command` で実行すること。
